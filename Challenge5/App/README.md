# App

Entry/root-level SwiftUI app flow.

Use this for files like:
- `RootView.swift`
- lightweight app routing only when needed

Do not add complex router architecture until Fian chooses it as a learning topic.
