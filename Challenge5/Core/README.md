# Core

Shared code only when it is genuinely reused.

Possible future contents:
- simple shared models
- formatting helpers
- utility functions

Avoid premature abstraction. Keep code near the feature until reuse is real.
